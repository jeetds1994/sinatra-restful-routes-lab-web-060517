<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1>Recipes</h1>
    <% @recipes.each do |obj| %>
    <%= obj.name %>
    <%= obj.ingredients %>
    <%= obj.cook_time %>
    <a href=<%= "/recipes/#{obj.id}" %>>Recipe Page</a>
    <% end %>
  </body>
</html>
